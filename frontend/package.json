{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "rm -rf ./out && rm -rf ../backend/template && next build && next export && mv ./out ../backend/template",
    "start": "next start"
  },
  "dependencies": {
    "next": "12.1.6",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}
